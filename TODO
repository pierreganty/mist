Ajouter PNML comme language d'input.

Verifier que les transfers passent bien.
Verifier si la place target doit aussi apparaitre comme source.
Regardez ce qui se passe avec le cas X' = Y  + 1
et peut etre essayer de le détecter.

Cherchez les fonctions qui sont syntaxiquement orpheline et nettoyer le code.
Remplacer la list_ist par une structure de liste générique.
Supprimer le champ NbElements ?
Fix a policy on emptines test, normalize and so on: each
function must deal with empty ist and return a normalized ist.
Redundant function GiveMeAPath ist_firstpath2array
ist_pre_of_all_transfer !!!!!


Créer une petite page web avec les données essentielles.
	Ce que fait l'outil.
	Contact.
	Code.
	Testsuite. (un gros tar.gz)
	ChangeLog des mises à jour. (aussi bien pour la testsuite
	que pour le code source)
	Liens utiles (ma page web, celle de Laurent).
